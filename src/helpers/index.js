/**
 * Helper functions
 * 
 * Misc helper functions to reduce code duplication within the app
 * @module helpers
 */

module.exports = {
    AuthHelper: require('./auth.helper'),
    FileHelper: require('./file.helper'),
    MailHelper: require('./mail.helper'),
    RegexHelper: require('./regex.helper')
}